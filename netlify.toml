[build]
  publish = "dist"
  command = "mkdir -p dist && echo 'Static site — no build step'"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-functions-install-core"